pre-commit:
  commands:
    lint:
      run: npm run lint
      stage_fixed: true

post-merge:
  only:
    - ref: main
  commands:
    install:
      run: npm install

output:
  - summary
  - failure
